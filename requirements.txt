pandas>=1.5.0
numpy>=1.23.0
scipy>=1.9.0
openpyxl>=3.0.10
matplotlib>=3.6.0
seaborn>=0.12.0
statsmodels>=0.13.0
gseapy>=1.0.4
biorosetta>=0.3.0
requests>=2.28.0
jupyter>=1.0.0
# Note: google.colab is used for file upload/download in Colab mode only.
# When running locally, replace cf.upload() / colab_files.upload() calls
# with pd.read_excel('path/to/file.xlsx') pointing to the data/ directory.
